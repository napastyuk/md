```javascript
{
'altKey':false, //была ли нажата клавиша alt
'bubbles':true, //будет ли событие всплывать
'button':0,  //кнопки, нажатые при запуске события мыши. При нажатии нескольких кнопок записывается только код превой кнопки мышиа
//0 - левая/главная кнопка
//1 - колесико/средняя/дополниельная кнопка
//2 - правая/контектстная кнопка
//при настройки мыши под левую руку , назначение соответсвенно меняется
'buttons':1, //кнопки, нажатые при запуске события мыши. При нажатии нескольких кнопок записывается сумма
//1 - левая/главная кнопка
//2 - правая/контектстная кнопка
//4 - колесико/средняя/дополниельная кнопка
//8 - клавиша кнопки назад
//16 - клавиша кнопки вперёд
'cancelBubble':false, //исторический синоним для bubbles
'cancelable':true, //является ли событие отменяемым
'clientX':30, //координаты курсора относительно текущего окна(не документа , а окна!).
'clientY':10, //координаты курсора относительно текущего окна.
'composed':true, //экспериментальное, будет ли событие всплывать за пределы shadow dom
'ctrlKey':false, //была ли нажата клавиша ctrl
'currentTarget':null, //элемент, на котором в данный момент сработал обработчик (до которого «доплыло» событие)
'defaultPrevented':false, //был ли вызван event.preventDefault() в текущем обработчике
'detail':1, //возвращает количество щелчком мыши сделаннным по элементу , за время заданное в системе под двойной клик
'eventPhase':0, // на какой фазе он сработал (погружение=1, всплытие=3).
'fromElement':null, //для совместимости с IE, синоним свойства relatedTarget.  Cсылка на объект, с которого двигался указатель мыши.
'isTrusted':true, //было ли событие сгенерированно js-м
'layerX':30, //устаревшее, координаты
'layerY':10, //тоже по Y
'metaKey':false, //была ли нажата клавиша Cmd для windows и Mac
'movementX':0, //разница между нынешним значением screenX и значением сгенерированным во время последнего mousemove
'movementY':0, //тоже по Y
'offsetX':22, //отступ курсора мыши по оси X от края целевого DOM узла
'offsetY':2, //тоже по Y
'pageX':30, //координаты курсора относительно документа
'pageY':10, //тоже по Y
'path':[div, body, html, document, Window], //массив элементов по цепочке иерархии DOM
'relatedTarget':null, //элемент, с которого пришла мышь , может быть null особенно если мышь пришла из-за пределов окна
'returnValue':true, //для совместимости с IE. свойство содержит false если в обработчике стоит отмена действий по умолчанию
'screenX':30, //координаты на экране , независимо от браузера
'screenY':101,//координаты на экране , независимо от браузера
'shiftKey':false, //была ли нажата клавиша shift
'sourceCapabilities':{firesTouchEvents: false}, //экспериментальное, объект с свойствами устройства на котором произошло событие
'srcElement':div, // для совместимости с IE, синоним свойства target
'target': div, //  элемент, на который пришла мышь, то есть на котором возникло событие
'timeStamp':35716.995, // время в мс, когда произошло событие
'toElement':div, // для совместимости с IE, синоним свойства relatedTarget. Cсылка на объект, к которому двигался указатель мыши.
'type':"mousedown", // строка с типом события который сработал на элементе
'view':Window {postMessage: ƒ, …}, //ссылка на текущий объект window
'which':1, //устаревшее, содержит цифровой код нажатой клавиши
/*
По свойству which удобно получать какая кнопка мышки сделала клик:
- event.which == 1 – левая кнопка
- event.which == 2 – средняя кнопка
- event.which == 3 – правая кнопка
*/
'x':30,//устаревшее, координаты
'y':10, //тоже по Y
'_proto__':MouseEvent //ссылка на прототип объекта
}
```
