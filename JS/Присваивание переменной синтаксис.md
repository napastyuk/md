```javascript
var x = 2, y = 2; // так можно, операция присваивания объединяется через оператор объединения ',' будет x=2 и y=2

var x = y = 2;  // так тоже можно хотя  и не рекомендуется(см ссылочные и примитивные типы)

var x, y;   //так можно, будет x=undefined, y=underfined
var a, b = c = 2;  // и даже совмещать , но важно что будет a = underfined, а=2, с =2

var x, var y; // а вот так сломается. потому что оператор var ожидает имя переменной и когда его получает то перестает обрабатывать остальное. Оператор , выполняется слева-направо т е сначала будет выполнен x, остальное проигнорируется и выпадет с ошибкой 
```
